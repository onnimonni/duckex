# SPDX-FileCopyrightText: 2025 Stas Muzhyk <sts@abc3.dev>
# SPDX-FileCopyrightText: 2025 Łukasz Niemier <~@hauleth.dev>
#
# SPDX-License-Identifier: Apache-2.0

# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
duckex-*.tar

# Rust binary artifacts
/target/

/priv/native/duckex
/priv/native/*.so
/tmp/

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# DuckDB database files
*.duckdb
*.duckdb.wal

# Sample Phoenix database files
sample_phoenix/*.ducklake
sample_phoenix/*.ducklake.files/
sample_phoenix/*.ducklake.wal

# Sample Phoenix applications
sample_phoenix/
sample_phoenix_postgres/

# Ecto directories (separate repos)
ecto/
ecto_sql/
ecto_duckdb/
